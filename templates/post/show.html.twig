{%extends 'base.html.twig'%}

{%block body%}
    {% for item in posts %}
    {% if item.id == id %}
    <h1>{{item.title}}</h1>
    <p>{{item.body}}</p>
    TAG : 
    {% for tag in item.tags %}
    <span>{{ tag}}</span>
    {% endfor %}
    {% endif %}
    {% endfor %}

{%endblock%}